default: # default connection
  host: 'localhost'
  port: 5432
  username: 'postgres'
  password: 'postgres'
  database: 'postgres'
  type: postgres
  synchronize: false,
  entities:
    - lib/models/*.ts
  migrations:
    - lib/migrations/*.ts
  cli:
    migrationsDir: lib/migrations
